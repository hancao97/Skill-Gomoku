# 听雨弈境 2.8.1 验证记录

验证日期：2026-09-13。环境：macOS、Apple M1 / 16 GB、Godot 4.7.2、Forward+ / Metal。其他操作系统及显卡尚未验证。

## 2.8.1 第四局神之一手与设置名称

「神之一手」的入口改为根据永久优势方的当前棋色显示，覆盖第二、第四局；第二局用过后，第四局可以再次发动。设置「特技」更名为「先手特技」，保留原配置键。

| 检查 | 本次结果 |
| --- | --- |
| 原有棋局规则 | 133 项通过 |
| 神之一手归属、换局及旧存档 | 103 项通过 |
| 第二、第四局真实鼠标与画面回放 | 51 项通过 |
| 设置控件与持久化回放 | 27 项通过 |

合计 **314 项检查通过，0 失败**。新增规则检查串联第二局发动、第三局对方正常获胜、第四局再次发动并以 3∶1 结束比赛；覆盖旧第四局存档恢复、黑方禁止发动、65 个交点变白、范围外保留和重复结算防护。第四局渲染回放从恢复存档开始，经双方普通落子后点击棋盒旁白子，完成同一手势演出与判胜，普通雷声重叠帧数为 **0**。

设置回放通过真实控件点击验证独立开关、配置保存与旧配置兼容，已检查「先手特技」名称及说明没有截断。最终导入、导出和回放日志无脚本错误或警告。声音文件、增益与手势动画沿用 2.8.0，本次未重新测量音频峰值。

结果位于 `verification/v2.8.1/`，包含规则日志、`hand-playback.json`、`settings-playback.json`、第二及第四局的技能阶段截图和设置截图。以下保留旧版验证基线。

## 2.8.0 手势动作与轮廓

「神之一手」的空中造型改为独立的 119 颗白子雕塑，连续银白轮廓描出腕部、掌缘和指缝，参考片尾手势完成收指、送腕、伸指、回腕和最终下点。65 点规则手印、触发归属、设置开关及存档规则保持一致。

| 检查 | 本次结果 |
| --- | --- |
| 原有规则 | 133 项通过 |
| 神之一手归属与结算 | 92 项通过 |
| 手势几何与事件顺序 | 10 项通过，采样 145 个连续姿态 |
| 真实鼠标与画面回放 | 38 项通过 |
| 四招与环境声音回放 | 49 项通过 |

合计 **322 项检查通过，0 失败**。手势几何检查包含连续轮廓可三角化、四条可见手指始终与掌面相连、棋子位置有效、食指独立展开、65 次落印、额外棋子消散，以及低帧率跨越多个阶段时的事件顺序。渲染回放检查送腕、回腕、指尖落点、纯白轮廓、棋盘结果及双方输入限制；技能期间普通雷声重叠帧数为 **0**。导入、回放与导出日志无脚本错误或警告。

实际 Master 录音 **62.72 秒**，采样峰值 **−1.999 dBFS**，4 倍重建真峰值 **−1.978 dBTP**，削波样本 **0**；19 份声音资源的峰值、边界及循环检查均通过。原有音频文件与增益保留，只将本招运指声移至发动后 2.72 秒，并在 4.10 秒指尖触盘时衔接冲击声。

结果位于 `verification/v2.8/`：`gesture.log`、`hand-playback.json`、`audio-playback.json`、`audio-measurements.json` 及七张技能阶段截图。逐张检查聚子、收指、伸指、回腕、触盘和书法；`divine-hand-demo.mp4` 为 Godot 30 FPS Movie Maker 录制后剪出的 **9.9 秒**实机演示，保留引擎音量，`divine-hand-preview.wav` 来自实际 Master 输出。普通回放采样处为 18–34 FPS，伸指处 31 FPS；固定帧录像不代表实时帧率保证。以下保留旧版验证基线。

## 2.7.0 神之一手

新增第二局黑棋盒旁的白子，只能由永久优势白方在自己的回合点击发动。65 个棋盘交点组成左下指势，空中另有错位掌面层构成立体大手；食指先下压，随后棋子依次落成白色手印。手形范围外的棋色不变，本局只结算一次胜利。取消、特技关闭、仅关闭特效、旧存档恢复和进入第三局均有对应处理。

- 原有规则：**133 项通过**；新增规则矩阵：**92 项通过**，覆盖五局、双方棋色、两类设置、空位和黑子转换、范围外保留及旧存档。
- 真实输入与画面回放：**28 项通过**，覆盖黑方无法触发、拖走／失焦／暂停取消、白子入口可见性、巨手聚合与指势、65 个实子落盘、书法、音效和特效关闭后的同一结果。技能期间雷声重叠帧数为 0。
- 声音回放：**49 项通过**，覆盖四招完整演出、17 段预加载 PCM、混音、静音、循环和隔离的过载段。19 份声音资源检查均通过。
- 原有雷电与技能互斥回放：**20 项通过、0 重叠帧**。本次合计 **322 项检查通过，0 失败**，最终回放与导出日志无脚本错误或警告。

本次实际 Master 录音长 **61.5253 秒**，采样峰值 **−1.999 dBFS**，4 倍重建真峰值 **−1.978 dBTP**，削波样本为 **0**。数值验证不代替主观听感评价。

结果位于 `verification/v2.7/`，包含 `hand-rules.log`、`hand-playback.json`、`audio-playback.json` 和 `audio-measurements.json`。实机逐张检查棋盒旁的白子、飞聚、成手、指尖触盘和「神之一手」四字。`divine-hand-demo.mp4` 为 Godot 30 FPS Movie Maker 录制并剪出的 8.8 秒完整技能片段，保留引擎声音，不作音量归一化；`divine-hand-preview.wav` 来自普通声音回放的实际 Master 输出。

在本机首次回放的聚子截图处出现 8 FPS，加入材质预热后同一采样处为 25 FPS、成手处 31 FPS；这些是单次截图附近的观察值，并非所有设备的帧率保证。以下各节保留旧版验证基线。

## 2.6.0 技能声音重制

替换八段技能与胜利声音，新增天地大同重落前的吸气声；挽弓按拉动程度改变张力，挽弓和满蓄力均有持续循环，松弓、取消及静音沿用淡出释放。

声音回放完成 **44 项检查，0 失败**，新增长按超过起音段仍持续发声、拉动张力变化、松回原位停止绷弦声检查，并覆盖三招完整混音、静音、循环接缝与压力叠加。雷电与技能互斥回放完成 **20 项检查，0 失败、0 重叠帧**。本次共 **64 项检查通过**，并测量 16 份活跃声音资源及引擎实际输出，均无削波或接缝检查失败。

报告位于 `verification/v2.6/`。`skills-preview.wav` 是实际 Master 录音中三招的 21.624 秒试听，按挽弓、月轮、天地大同排列；片段之间留 0.7 秒，剪辑首尾仅做 15 ms 淡入淡出，未调整试听增益。以下保留以前版本的视觉与玩法验证记录。

## 2.5.3 预览与实子对齐

复现了此前只补偿预览、未补偿实子的差异：在同一测试窗口，落子后模型中心移动约 4.622 像素。现在预览、实子及蓄力模型共用显示定位，逻辑棋位仍在原交点；模型随镜头和动画补偿透视。

`--verify --isolated-replay --hover-check` 完成 **122 项检查，0 失败**。测量投影后的实际网格轮廓，覆盖黑白棋、1280×800 与 1920×1200、中央及四角共 20 组真实鼠标按下与释放。预览到实子轮廓中心偏移均为 **0 像素**，轮廓中心距棋盘目标交点最大约 **0.244 像素**。另核对相邻四子排列，保留点击前后的实机画面，以及即时跟随、静止、占位和菜单隐藏等检查。报告与截图位于 `verification/v2.5.3/`。

反馈回放另完成 **75 项检查，0 失败**，覆盖对方棋子震起、回落、双方特效归属、长按蓄力、取消与全盘填黑；技能重叠帧数为 0。

三种技能完整演出回放完成 **36 项检查，0 失败**，包括黑棋拉弓、射线补黑、白棋完整 5×5、棋盘长按与 225 点填黑，以及放置动画和落子声触盘时机。本次合计 **233 项检查通过**。声音素材、混音和雷鸣增益沿用 2.5.2 的验证结果。

## 2.5.2 雷鸣音量

普通落雷的两段雷声播放增益从 −6 dB 降为 −9 dB。声音专项回放完成 **40 项检查，0 失败，技能重叠帧数 0**；实际 Master 录音和 15 份声音资源检查均通过，削波样本为 0。完整报告与当时的测量数据留存于 `verification/v2.5.2/`。

## 2.5.1 落点预览修正

2.5.1 调整了预览定位与鼠标移动时的更新，移除额外抬高和周期浮动；当时仅补偿了预览，实子差异已在 2.5.3 修正。

`--verify --isolated-replay --hover-check` 完成 **59 项检查，0 失败**。两种棋色、1280×800 与 1920×1200 两种窗口尺寸、中央及四角共 20 个投影样本，预览包围盒中心距目标交点最大约 **0.639 像素**。同时确认即时跟随、静止不漂移、点击落在预览交点、换色、菜单和占位隐藏、离开窗口、关闭特效后仍可预览。实机截图与报告位于 `verification/v2.5.1/`。

以下保留 2.5.0 的完整功能验证基线，本次重跑的项目在上文单列。

## 2.5.0 规则与设置基线

| 检查 | 结果 | 可复现入口 |
| --- | --- | --- |
| 棋局规则 | 133 项通过，0 失败 | `tests/test_rules.gd` |
| 设置与实际鼠标回放 | 27 项通过，0 失败 | `--verify --settings-check` |
| 悬浮、震子与蓄力反馈 | 75 项通过，0 失败；技能重叠帧数 0 | `--verify --feedback-check` |
| 三种技能完整实机演出 | 36 项通过，0 失败 | `--verify` |
| 雷电与技能互斥 | 20 项通过，0 失败；重叠帧数 0 | `--verify --effect-priority` |

覆盖永久优势归属与换色、普通玩家无特效、T 形四向识别、白棋等待回合、射线包含空位、月轮完整 5×5、棋盘长按与 225 点填黑、三胜结束、存档恢复及重复结算防护。

设置回放点击真实控件，验证两开关独立保存、旧配置默认开启、关闭特效后技能仍转换棋盘、关闭特技后长按和成环恢复普通落子、中途关闭取消手势，以及重新开启后优势不转移。HUD 和设置页不泄露技能触发方法。

反馈回放覆盖双方悬浮棋色、空位吸附、占位和菜单隐藏、窗口离开、特效关闭后仍有落点预览；第三至第五局分别检查优势归属、对方棋子离盘、己方不动、完整恢复原位和棋局不受影响。中途关闭特效会立即恢复棋子。

蓄力的早期、中段、满蓄力均有实机截图。回放对比有无墨流的同一场景，避开中央棋子，在外侧区域统计变暗的采样点：6 → 1753 → 3327；距离中心更远的墨纹采样点为 3 → 151 → 863。满蓄力时变亮的采样点只有 6，确认增强的是墨色纹理，且向外延伸；中性反光只照亮黑子附近。回放还检查满蓄力等待、音效结束后的取消、拖走、设置、失焦、重新蓄力与正常释放。

已逐张复查黑白落雷、震子、黑色蓄力、黑棋挽弓与天地大同、白棋月轮及书法颜色。黑棋弓迹、雷电与书法主色为墨黑；白棋月轮和书法为银白。棋形不提前显示技能提示。

雷电回放覆盖第三局普通落雷，以及蓄力、拉弓、月轮、天地大同、取消手势和换局之间的互斥。当前输出目录为 `verification/v2.5/`，包含 `playback-result.json`、`feedback-result.json`、`settings-playback.json`、`effect-priority-result.json` 和实机截图。

合计 291 项检查通过。图形回放使用 `--isolated-replay`：保持测试窗口可绘制，隔离桌面真实输入；脚本仍通过 `Viewport.push_input()` 测试游戏与控件。窗口退出、失焦的取消处理由脚本显式调用同一函数，避免桌面操作干扰自动回放。

## 声音测量

2.6.0 完成 44 项声音回放检查，包含持续蓄力与张力循环、新技能与胜利声音、普通落雷、对方棋子落回声，以及环境音混音。下列为本次引擎输出的实测数据。

| 测量 | 结果 |
| --- | --- |
| 活跃声音资源 | 16 份，含 14 段 48 kHz 双声道短音效、雨声和背景乐 |
| 音效素材削波样本 | 0 |
| 实际 Master 录音长度 | 52.5653 秒 |
| 实际混音削波样本 | 0 |
| 实际混音采样峰值 | −1.999 dBFS |
| 4 倍 sinc 重建真峰值 | −1.976 dBTP |

录音覆盖普通落子、第三局雷鸣、三种技能、胜利、手势取消、快速静音、循环接缝与隔离的过载压力段。压力段同时叠加 8 个重击和 4 个雷声，用于验证总线限幅；这不是正常游戏行为。录音来自引擎 Master 总线，测量前没有归一化。

数值验证说明测试场景中未发生削波，不代替听感评估。测量输出为 `verification/v2.6/audio-measurements.json`，同目录包含 `audio-playback.json`、`engine-mix.wav` 与三招试听。设计与复现方法见 [AUDIO.md](AUDIO.md) 和 [资源重建说明](docs/ASSET_BUILD.md)。

## 本机应用与数据

2.8.1 已完成 ZIP 导出、签名校验与本机替换，安装文件逐一与导出包核对一致。安装版已确认「先手特技」名称，并恢复原有第三局开局、墨客 2∶0 听雨、空棋盘的状态；更新与继续棋局后，存档及设置文件摘要均保持一致。旧版备份为 ZIP，临时安装目录已清理，Spotlight 只收录 `/Applications/听雨弈境.app` 一个应用。记录位于 `verification/v2.8.1/installation.json`。

2.8.0 已完成 macOS 导出和本机临时签名校验，安装版与导出包的 PCK 摘要相同。正常退出旧应用后再备份与替换，棋局和设置文件与更新前摘要一致；新版已启动，并通过“续上未尽之局”恢复到第二局结束、墨客 2∶0 听雨的同一棋盘与结算画面，恢复后文件摘要仍相同。记录位于 `verification/v2.8/installation.json`，旧应用另留本地备份。

所有回放使用 `--verify`，不覆盖正常玩家的棋局；设置持久化检查写入测试目录。`verification/` 中的录音、测试配置、截图和本地发布记录不提交 Git，也不进入导出应用。仓库保留可复现脚本及选取的 [悬浮棋子](docs/images/gameplay.png)、[设置](docs/images/settings.png)、[震子](docs/images/recoil.png)、[满蓄力](docs/images/charge.png)、[墨黑挽弓](docs/images/bow.png)、[银白月轮](docs/images/moon.png) 截图。

最终通过的规则、设置、技能、反馈与互斥回放日志均无脚本错误或警告。测试与音频分析自动建立输出目录，无需预先复制本机测试文件。

启动、测试与导出命令见 [README](README.md)。
