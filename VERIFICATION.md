# 听雨弈境 2.4.0 验证记录

验证日期：2026-09-12。环境：macOS、Apple M1 / 16 GB、Godot 4.7.2、Forward+ / Metal。其他操作系统及显卡尚未验证。

## 当前规则与设置

| 检查 | 结果 | 可复现入口 |
| --- | --- | --- |
| 棋局规则 | 133 项通过，0 失败 | `tests/test_rules.gd` |
| 设置与实际鼠标回放 | 27 项通过，0 失败 | `--verify --settings-check` |
| 悬浮、震子与蓄力反馈 | 72 项通过，0 失败；技能重叠帧数 0 | `--verify --feedback-check` |
| 雷电与技能互斥 | 20 项通过，0 失败；重叠帧数 0 | `--verify --effect-priority` |

覆盖永久优势归属与换色、普通玩家无特效、T 形四向识别、白棋等待回合、射线包含空位、月轮完整 5×5、棋盘长按与 225 点填黑、三胜结束、存档恢复及重复结算防护。

设置回放点击真实控件，验证两开关独立保存、旧配置默认开启、关闭特效后技能仍转换棋盘、关闭特技后长按和成环恢复普通落子、中途关闭取消手势，以及重新开启后优势不转移。HUD 和设置页不泄露技能触发方法。

反馈回放覆盖双方悬浮棋色、空位吸附、占位和菜单隐藏、窗口离开、特效关闭后仍有落点预览；第三至第五局分别检查优势归属、对方棋子离盘、己方不动、完整恢复原位和棋局不受影响。中途关闭特效会立即恢复棋子。

蓄力的早期、中段、满蓄力均有实机截图。固定画面区域的平均亮度依次为 0.558、0.695、0.936；远离棋盘反光点的高亮采样点从 0 增至 1307，确认光束本身逐渐展开。回放还检查满蓄力等待、音效结束后的长按取消、拖走、设置、失焦与正常释放。

雷电回放覆盖第三局普通落雷，以及蓄力、拉弓、月轮、天地大同、取消手势和换局之间的互斥。当前输出目录为 `verification/v2.4/`，包含 `feedback-result.json`、`settings-playback.json`、`effect-priority-result.json` 和实机截图。

## 声音测量

2.4 声音专项完成 40 项回放检查，新增对方棋子震起后的整组落回声录制，音频资源和混音总线保留原有输出保护。

| 测量 | 结果 |
| --- | --- |
| 活跃声音资源 | 15 份，含 13 段 48 kHz 双声道短音效、雨声和背景乐 |
| 音效素材削波样本 | 0 |
| 实际 Master 录音长度 | 47.7013 秒 |
| 实际混音削波样本 | 0 |
| 实际混音采样峰值 | −1.999 dBFS |
| 4 倍 sinc 重建真峰值 | −1.976 dBTP |

录音覆盖普通落子、第三局雷鸣、三种技能、胜利、手势取消、快速静音、循环接缝与隔离的过载压力段。压力段同时叠加 8 个重击和 4 个雷声，用于验证总线限幅；这不是正常游戏行为。录音来自引擎 Master 总线，测量前没有归一化。

数值验证说明测试场景中未发生削波，不代替不同扬声器上的主观听感评估。测量输出为 `verification/v2.4/audio-measurements.json`，回放输出为 `audio-playback.json` 与 `engine-mix.wav`。重新测量会覆盖这些本地报告。设计与复现方法见 [AUDIO.md](AUDIO.md) 和 [资源重建说明](docs/ASSET_BUILD.md)。

## 本机应用与数据

2.4.0 已完成 macOS 导出和本机临时签名校验，安装包与导出包的 PCK 内容一致。启动安装版后继续已有棋局，通过实际鼠标输入确认普通玩家的白棋悬浮预览正常显示，未替玩家落子。升级保留棋局、声音和画质偏好。

所有回放使用 `--verify`，不覆盖正常玩家的棋局；设置持久化检查写入测试目录。`verification/` 中的录音、测试配置、截图和本地发布记录不提交 Git，也不进入导出应用。仓库保留可复现脚本及选取的 [悬浮棋子](docs/images/gameplay.png)、[设置](docs/images/settings.png)、[震子](docs/images/recoil.png)、[满蓄力](docs/images/charge.png) 截图。

2.4 的导入、渲染回放和导出日志均无脚本错误或警告。测试与音频分析自动建立输出目录，无需预先复制本机测试文件。

启动、测试与导出命令见 [README](README.md)。
