# 听雨弈境 2.3.0 验证记录

验证日期：2026-09-12。环境：macOS、Apple M1 / 16 GB、Godot 4.7.2、Forward+ / Metal。其他操作系统及显卡尚未验证。

## 当前规则与设置

| 检查 | 结果 | 可复现入口 |
| --- | --- | --- |
| 棋局规则 | 133 项通过，0 失败 | `tests/test_rules.gd` |
| 设置与实际鼠标回放 | 27 项通过，0 失败 | `--verify --settings-check` |
| 雷电与技能互斥 | 20 项通过，0 失败；重叠帧数 0 | `--verify --effect-priority` |

覆盖永久优势归属与换色、普通玩家无特效、T 形四向识别、白棋等待回合、射线包含空位、月轮完整 5×5、棋盘长按与 225 点填黑、三胜结束、存档恢复及重复结算防护。

设置回放点击真实控件，验证两开关独立保存、旧配置默认开启、关闭特效后技能仍转换棋盘、关闭特技后长按和成环恢复普通落子、中途关闭取消手势，以及重新开启后优势不转移。HUD 和设置页不泄露技能触发方法。

雷电回放覆盖第三局普通落雷，以及蓄力、拉弓、月轮、天地大同、取消手势和换局之间的互斥。当前输出目录为 `verification/v2.3/`，包含 `settings-playback.json`、`effect-priority-result.json` 和实机截图。

## 声音测量

2.2 声音专项完成 40 项回放检查；2.3 沿用该音频资源和混音总线。

| 测量 | 结果 |
| --- | --- |
| 活跃声音资源 | 15 份，含 13 段 48 kHz 双声道短音效、雨声和背景乐 |
| 音效素材削波样本 | 0 |
| 实际 Master 录音长度 | 48.896 秒 |
| 实际混音削波样本 | 0 |
| 实际混音采样峰值 | −1.999 dBFS |
| 4 倍 sinc 重建真峰值 | −1.98 dBTP |

录音覆盖普通落子、第三局雷鸣、三种技能、胜利、手势取消、快速静音、循环接缝与隔离的过载压力段。压力段同时叠加 8 个重击和 4 个雷声，用于验证总线限幅；这不是正常游戏行为。录音来自引擎 Master 总线，测量前没有归一化。

数值验证说明测试场景中未发生削波，不代替不同扬声器上的主观听感评估。测量输出为 `verification/v2.2/audio-measurements.json`，回放输出为 `audio-playback.json` 与 `engine-mix.wav`。重新测量会覆盖这些本地报告。设计与复现方法见 [AUDIO.md](AUDIO.md) 和 [资源重建说明](docs/ASSET_BUILD.md)。

## 本机应用与数据

2.3.0 已完成 macOS 导出和本机临时签名校验，从安装位置启动并检查设置页。新增开关默认开启，已有声音、画质偏好和棋局保留。

所有回放使用 `--verify`，不覆盖正常玩家的棋局；设置持久化检查写入测试目录。`verification/` 中的录音、测试配置、截图和本地发布记录不提交 Git，也不进入导出应用。仓库保留可复现脚本及选取的 [棋盘](docs/images/gameplay.png)、[设置](docs/images/settings.png) 截图。

源码发布前，从 Git 暂存内容建立不含 `.godot/` 和 `verification/` 的干净副本：首次资源导入无错误或警告，133 项规则检查、27 项设置回放和 15 份音频资源分析均通过。回放与音频分析能自动建立输出目录，无需本机原有缓存或测试文件。

启动、测试与导出命令见 [README](README.md)。
