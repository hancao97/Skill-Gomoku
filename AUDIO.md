# 声音与混音 · 2.6

普通落子与雷鸣保留现有录音。三种技能各自使用绷弦、破风、金属旋转、低沉聚气与冲击层，按画面阶段编排；2.6 替换八段旧技能和胜利声音，并新增重落前的吸气声。

- 短音效：14 段 48 kHz / 16 bit / 双声道 PCM，Godot 导入压缩关闭，统一预加载。新制九段声音保留至少 3 dB 重建峰值余量；落子与雷鸣沿用此前至少 2 dB 的余量。源文件包含录音和音效设计元素的高质量 MP3 预览，转换为 WAV 不恢复源文件已丢失的信息。
- 雨声：60 秒立体声，4 秒等功率交叉淡化；背景乐维持原有独立开关。两条环境音从静音渐入，避免读取保存设置前出声。
- 路由：Music、Rain、Effects 分组；Thunder 送入 Effects。Master 先清理 25 Hz 以下的能量，再用预读限幅器保护输出，预增益 −2 dB、上限 −2 dBFS、释放 120 ms。常规音量保留余量，仅过大的叠加触发限幅。
- 落雷音量：雷鸣播放增益为 −9 dB，2.5.2 较此前降低 3 dB；两段交替雷声使用相同增益。
- 手势取消与换局：45 ms 音量淡出后停止播放；连续雷声交接 120 ms。雷声切入技能使用引擎的采样插值停止，并为新的技能声音保留 45 ms 间隔。取消尚未发声的手势也会取消延迟播放。
- 设置：雨声 180 ms、音乐 240 ms 渐变；设置增益与技能避让相乘，避让恢复不会覆盖静音。停止已有音效，重新打开时不会恢复旧的大招尾声。
- 退出：先淡出，再停止循环并等待混音线程释放。静音收尾不会保存成用户的声音偏好。
- 震子落回：整组共用一次 −16 dB 的触盘声，不为每颗棋子单独叠加声音；取消震动时不补播落回声。长按期间聚气声持续循环，释放或取消会停止并清理声音引用。

## 三种技能

| 阶段 | 声音与时序 |
| --- | --- |
| 挽弓 | 0.50 秒绷弦起音后进入 0.64 秒张力循环；拉动越深，音量和音高逐渐提高。松回原位会淡出，继续拉动可重新绷弦。放箭时播放弦响与破风，飞出棋盘后接独立的短穿透声。 |
| 月轮 | 六次逐渐加快的左右旋转声贯穿 2.05 秒成环和下落，在触盘时切入清亮的金属扩散与余响。此招不用黑棋的低沉冲击。 |
| 天地大同 | 棋盘长按开始 0.32 秒后聚气，1.68 秒起音达到满蓄力，随后维持 0.80 秒循环。松手后用 0.87 秒吸气声衔接升起和重落，给触盘前留出短间隔；触盘瞬间才播放重击、低频主体和气浪。 |
| 普通胜利 | 2.20 秒的收势与共鸣替代长鼓声；技能胜利沿用各自演出收尾。 |

所有循环使用每个播放实例独立的 `AudioStreamWAV`，不修改预加载资源；取消、静音、菜单和换局均通过原有淡出流程释放。技能触发方法继续隐藏在游戏 HUD 之外。

限幅采用 Godot 的 [AudioEffectHardLimiter](https://docs.godotengine.org/en/stable/classes/class_audioeffecthardlimiter.html)。当前技能制作脚本为 `tools/build_skill_audio.py`，录音与音效设计元素来自 Ali_6868、DustyWind 和 AudioPapkin 的 CC0 作品，辅以可复现的共鸣与周期声场；页面链接和处理方式见 [音频署名](assets/licenses/audio-v2.txt)。`tools/build_audio_v2.py` 保留基础声音与旧版技能重建，`tools/build_thunder.py` 单独处理雷鸣，`tools/audio_dsp.py` 和 `tools/verify_audio.py` 用于输出保护与测量。

## 验证

`verification/v2.6/audio-measurements.json` 包含 16 份活跃素材的峰值、直流偏移、起止、环境与手势循环接缝检查，以及引擎实际 Master 输出的 4 倍重建真峰值和削波样本数。测试输出不做增益归一化。

`tests/playback_audio.gd` 在真实游戏场景中触发普通落子、第三局雷鸣、拉弓、白棋 5×5、天地大同、胜利、取消手势、快速静音和循环接缝；另设明确隔离的过载段，叠加 8 个重击和 4 个雷声来验证输出保护。这种叠加只存在于测试中。记录直接取自 Godot Master 总线，使用音频采样时钟标记分段；Dummy 驱动避免测试音频打扰正在使用电脑的人。

`tests/playback_effect_priority.gd` 检查雷电和技能的互斥；`tests/playback_feedback.gd` 检查悬浮、震子与蓄力光束；`tests/test_rules.gd` 检查棋形、回合、归属和棋盘转换。设置、声音及互斥回放按当前工程版本建立输出目录。当前结果见 [VERIFICATION.md](VERIFICATION.md)，这些本地测试产物均由 Git 忽略。

录音后执行 `python tools/preview_skill_audio.py`，可从实际 Master 混音截取三招试听。片段只在剪辑边界加 15 ms 淡入淡出，保持游戏输出音量，另附每招起止时间；不会把压力测试段放入试听。
